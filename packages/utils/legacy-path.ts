import { fileURLToPath } from 'url'
import { dirname } from 'path'

export const get__filename = (): string => {
  return __filename
}

export const get__dirname = (): string => {
  return __dirname
}
